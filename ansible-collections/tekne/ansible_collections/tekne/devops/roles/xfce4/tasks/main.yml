# SPDX-License-Identifier: MIT-0
---
# tasks file for ./ansible-role-xfce4/

# =============================================================================
# Hostname from /etc/hostname
# =============================================================================

- name: Read hostname from /etc/hostname
  ansible.builtin.slurp:
    src: /etc/hostname
  register: hostname_file
  ignore_errors: true
  tags:
    - xfce4
    - lightdm

- name: Set hostname fact from /etc/hostname
  ansible.builtin.set_fact:
    hostname_from_etc: "{{ (hostname_file.content | b64decode | trim) if hostname_file is succeeded else '' }}"
  tags:
    - xfce4
    - lightdm

# =============================================================================
# Network Connectivity Check
# =============================================================================

- name: Wait for network connectivity before package installation
  ansible.builtin.command:
    cmd: ping -c 1 -W 2 archlinux.org
  register: network_check
  until: network_check.rc == 0
  retries: 30
  delay: 2
  changed_when: false
  when: not install_chroot_phase | bool
  tags:
    - xfce4
    - network
    - packages

# =============================================================================
# Package Installation - All Hosts
# =============================================================================

- name: Install XFCE4 package groups
  community.general.pacman:
    name: "{{ xfce4_groups }}"
    state: present
    extra_args: '--noconfirm --needed'
  register: xfce4_groups_install_result
  tags:
    - xfce4
    - packages

- name: Install XFCE4 packages
  community.general.pacman:
    name: "{{ xfce4_packages }}"
    state: present
    extra_args: '--noconfirm --needed'
  register: xfce4_install_result
  tags:
    - xfce4
    - packages

# =============================================================================
# LightDM Installation - ASTER Only
# =============================================================================

- name: Install LightDM packages (ASTER only)
  community.general.pacman:
    name: "{{ xfce4_lightdm_packages }}"
    state: present
    extra_args: '--noconfirm --needed'
  register: lightdm_install_result
  when: (hostname_from_etc | default('') | upper) in xfce4_lightdm_hosts
  tags:
    - xfce4
    - lightdm
    - packages

- name: Set LightDM greeter to slick greeter (ASTER only)
  ansible.builtin.replace:
    path: /etc/lightdm/lightdm.conf
    regexp: '^#greeter-session=example-gtk-gnome'
    replace: 'greeter-session=lightdm-slick-greeter'
  when: (hostname_from_etc | default('') | upper) in xfce4_lightdm_hosts
  tags:
    - xfce4
    - lightdm
    - config

- name: Enable LightDM service without starting (ASTER only)
  ansible.builtin.systemd:
    name: "{{ xfce4_lightdm_service }}"
    enabled: true
    state: stopped
  when: (hostname_from_etc | default('') | upper) in xfce4_lightdm_hosts
  tags:
    - xfce4
    - lightdm
    - service

# =============================================================================
# XFCE backgrounds and slick-greeter (LightDM host only)
# =============================================================================

- name: Ensure XFCE backgrounds directory exists
  ansible.builtin.file:
    path: "{{ xfce4_backgrounds_dir }}"
    state: directory
    mode: '0775'
  when: (hostname_from_etc | default('') | upper) in xfce4_lightdm_hosts
  tags:
    - xfce4
    - lightdm
    - backgrounds

- name: Find all files in backgrounds dir
  ansible.builtin.find:
    paths: "{{ xfce4_backgrounds_dir }}"
    recurse: yes
    file_type: any
  register: found_items

- name: Remove existing contents of XFCE backgrounds directory
  ansible.builtin.file:
    path: "{{ xfce4_backgrounds_dir }}"
    state: absent
  loop: "{{ found_items.files }}"
  when: found_items.files | length >0
  tags:
    - xfce4
    - lightdm
    - backgrounds

- name: Clone tekne-ops/backgrounds repository
  ansible.builtin.git:
    repo: "{{ xfce4_backgrounds_repo }}"
    dest: /tmp/ansible_xfce4_backgrounds_clone
    depth: 1
    force: true
  when: (hostname_from_etc | default('') | upper) in xfce4_lightdm_hosts
  tags:
    - xfce4
    - lightdm
    - backgrounds

- name: Create backgrounds directory
  ansible.builtin.file:
    path: "{{ xfce4_backgrounds_dir }}"
    state: directory
    owner: root
    group: root
    mode: 775

- name: Copy backgrounds repo content to XFCE backgrounds directory
  ansible.builtin.shell: "cp -r /tmp/ansible_xfce4_backgrounds_clone/* {{ xfce4_backgrounds_dir }}/"
  when: (hostname_from_etc | default('') | upper) in xfce4_lightdm_hosts
  tags:
    - xfce4
    - lightdm
    - backgrounds

- name: Remove temporary backgrounds clone
  ansible.builtin.file:
    path: /tmp/ansible_xfce4_backgrounds_clone
    state: absent
  when: (hostname_from_etc | default('') | upper) in xfce4_lightdm_hosts
  tags:
    - xfce4
    - lightdm
    - backgrounds

- name: Copy slick-greeter.conf to LightDM config
  ansible.builtin.copy:
    src: "{{ xfce4_backgrounds_dir }}/slick-greeter.conf"
    dest: /etc/lightdm/slick-greeter.conf
    remote_src: true
    owner: root
    group: root
    mode: '0644'
  when: (hostname_from_etc | default('') | upper) in xfce4_lightdm_hosts
  tags:
    - xfce4
    - lightdm
    - slick-greeter

- name: Set background in slick-greeter.conf
  ansible.builtin.replace:
    path: /etc/lightdm/slick-greeter.conf
    regexp: '^background=.*'
    replace: "background={{ xfce4_slick_greeter_background }}"
  when: (hostname_from_etc | default('') | upper) in xfce4_lightdm_hosts
  tags:
    - xfce4
    - lightdm
    - slick-greeter

# =============================================================================
# User Configuration
# =============================================================================

- name: Get user info for home directories
  ansible.builtin.getent:
    database: passwd
    key: "{{ item.name }}"
  loop: "{{ users }}"
  loop_control:
    label: "{{ item.name }}"
  register: user_info
  tags:
    - xfce4
    - config

- name: Create xdg-desktop-portal config directory for each user
  ansible.builtin.file:
    path: "{{ item.ansible_facts.getent_passwd[item.item.name][4] }}/.config/xdg-desktop-portal"
    state: directory
    owner: "{{ item.item.name }}"
    group: "{{ item.item.group | default(default_group) }}"
    mode: '0755'
  loop: "{{ user_info.results }}"
  loop_control:
    label: "{{ item.item.name }}"
  tags:
    - xfce4
    - config

- name: Create themes directory for each user
  ansible.builtin.file:
    path: "{{ item.ansible_facts.getent_passwd[item.item.name][4] }}/.themes"
    state: directory
    owner: "{{ item.item.name }}"
    group: "{{ item.item.group | default(default_group) }}"
    mode: '0755'
  loop: "{{ user_info.results }}"
  loop_control:
    label: "{{ item.item.name }}"
  tags:
    - xfce4
    - config
    - themes

- name: Extract minimal-grey2 theme for each user
  ansible.builtin.unarchive:
    src: 98942-minimal-grey2.tar
    dest: "{{ item.ansible_facts.getent_passwd[item.item.name][4] }}/.themes"
    owner: "{{ item.item.name }}"
    group: "{{ item.item.group | default(default_group) }}"
    creates: "{{ item.ansible_facts.getent_passwd[item.item.name][4] }}/.themes/Minimal-Grey2"
  loop: "{{ user_info.results }}"
  loop_control:
    label: "{{ item.item.name }}"
  tags:
    - xfce4
    - config
    - themes

- name: Copy xfce-portals.conf for each user
  ansible.builtin.copy:
    src: xfce-portals.conf
    dest: "{{ item.ansible_facts.getent_passwd[item.item.name][4] }}/.config/xdg-desktop-portal/xfce-portals.conf"
    owner: "{{ item.item.name }}"
    group: "{{ item.item.group | default(default_group) }}"
    mode: '0644'
  loop: "{{ user_info.results }}"
  loop_control:
    label: "{{ item.item.name }}"
  tags:
    - xfce4
    - config
    - portals

- name: Create picom config directory
  ansible.builtin.file:
    path: "{{ item.ansible_facts.getent_passwd[item.item.name][4] }}/.config/picom"
    state: directory
    owner: "{{ item.item.name }}"
    group: "{{ item.item.group | default(default_group) }}"
    mode: '0755'
  loop: "{{ user_info.results }}"
  loop_control:
    label: "{{ item.item.name }}"
  when: item.item.name == xfce4_picom_user
  tags:
    - xfce4
    - config
    - picom

- name: Copy picom.conf
  ansible.builtin.copy:
    src: picom.conf
    dest: "{{ item.ansible_facts.getent_passwd[item.item.name][4] }}/.config/picom/picom.conf"
    owner: "{{ item.item.name }}"
    group: "{{ item.item.group | default(default_group) }}"
    mode: '0644'
  loop: "{{ user_info.results }}"
  loop_control:
    label: "{{ item.item.name }}"
  when: item.item.name == xfce4_picom_user
  tags:
    - xfce4
    - config
    - picom

# =============================================================================
# System Configuration
# =============================================================================

- name: Copy gnome-keyring.portal to system portals directory
  ansible.builtin.copy:
    src: gnome-keyring.portal
    dest: /usr/share/xdg-desktop-portal/portals/gnome-keyring.portal
    owner: root
    group: root
    mode: '0644'
  tags:
    - xfce4
    - config
    - portals

- name: Copy ACPI handler script
  ansible.builtin.copy:
    src: handler.sh
    dest: /etc/acpi/handler.sh
    owner: root
    group: root
    mode: '0755'
  tags:
    - xfce4
    - config
    - acpi

# =============================================================================
# Verification - All Hosts
# =============================================================================

- name: Verify XFCE4 package groups are installed
  ansible.builtin.command:
    cmd: "pacman -Qg {{ item }}"
  loop: "{{ xfce4_groups }}"
  register: xfce4_group_verify
  changed_when: false
  failed_when: xfce4_group_verify.rc != 0
  tags:
    - xfce4
    - verify

- name: Verify XFCE4 packages are installed
  ansible.builtin.command:
    cmd: "pacman -Q {{ item }}"
  loop: "{{ xfce4_packages }}"
  register: xfce4_package_verify
  changed_when: false
  failed_when: xfce4_package_verify.rc != 0
  tags:
    - xfce4
    - verify

- name: Display XFCE4 installation result
  ansible.builtin.debug:
    msg: >-
      XFCE4 groups installed: {{ xfce4_groups_install_result is success }},
      XFCE4 packages installed: {{ xfce4_install_result is success }}
  tags:
    - xfce4
    - verify

# =============================================================================
# Verification - ASTER Only
# =============================================================================

- name: Verify LightDM packages are installed (ASTER only)
  ansible.builtin.command:
    cmd: "pacman -Q {{ item }}"
  loop: "{{ xfce4_lightdm_packages }}"
  register: lightdm_package_verify
  changed_when: false
  failed_when: lightdm_package_verify.rc != 0
  when: (hostname_from_etc | default('') | upper) in xfce4_lightdm_hosts
  tags:
    - xfce4
    - lightdm
    - verify

- name: Check LightDM service is enabled (ASTER only)
  ansible.builtin.command:
    cmd: "systemctl is-enabled {{ xfce4_lightdm_service }}"
  register: lightdm_enabled_check
  changed_when: false
  failed_when: lightdm_enabled_check.rc != 0
  when: (hostname_from_etc | default('') | upper) in xfce4_lightdm_hosts
  tags:
    - xfce4
    - lightdm
    - verify

- name: Check LightDM service is not running (ASTER only)
  ansible.builtin.command:
    cmd: "systemctl is-active {{ xfce4_lightdm_service }}"
  register: lightdm_active_check
  changed_when: false
  failed_when: false
  when: (hostname_from_etc | default('') | upper) in xfce4_lightdm_hosts
  tags:
    - xfce4
    - lightdm
    - verify

- name: Display LightDM verification results (ASTER only)
  ansible.builtin.debug:
    msg: |
      LightDM Status on {{ hostname_from_etc }}:
      ==================================
      Packages installed: {{ lightdm_install_result is success }}
      Service enabled: {{ lightdm_enabled_check.stdout | default('N/A') }}
      Service running: {{ lightdm_active_check.stdout | default('N/A') }} (should be 'inactive')
  when: (hostname_from_etc | default('') | upper) in xfce4_lightdm_hosts
  tags:
    - xfce4
    - lightdm
    - verify
