# SPDX-License-Identifier: MIT-0
---
# defaults file for ansible-role-nftables
# Hostname for server vs workstation config (from playbook hostname or ansible_hostname)
nftables_hostname: "{{ hostname | default(ansible_hostname) | upper }}"
