---
collections:
  - name: community.general
  - name: ansible.posix
  # Local monorepo (editable source under ../ansible-collections/tekne/devops)
  - name: tekne.devops
    type: dir
    source: /media/ansible-collections/tekne/devops

  # Remote Git (use instead of dir for CI / fresh clone without local collection tree)
  # - name: >-
  #     git+https://github.com/tekne-ops/ansible-collections.git#/tekne/devops
  #   type: git
  #   version: main
